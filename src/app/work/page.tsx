import { WorkOrbit } from "@/components/sections/WorkOrbit";

export const metadata = {
  title: "Work — Syed Abbas",
  description: "Fashion, fitness, editorial, and brand work by Syed Abbas.",
};

export default function WorkPage() {
  return (
    <main className="bg-background">
      <WorkOrbit />
    </main>
  );
}
